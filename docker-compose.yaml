services:
  db:
    image: postgres
    container_name: cubesat_postgres
    restart: always
    environment:
      POSTGRES_USER: cubesat_admin
      POSTGRES_PASSWORD: 14dQnsc14F3nQxjw42nc2Sdjc
      POSTGRES_DB: cubesat_telemetry
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: